# LiveKit config for NotDiscord voice/video.
# API keys come from the LIVEKIT_KEYS environment variable (see docker-compose.yml).
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true
